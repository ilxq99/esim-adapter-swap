
import { Toaster } from "@/components/ui/toaster";
import {text-primary" />
                 Toaster as<a href="mailto:info Sonner } from "@/components/ui/@switchesim.rusonner";
import { TooltipProvider" className="text-gray-400 hover } from "@/components:text-white transition/ui/tooltip";-colors">
                
import { QueryClient  info@switches, QueryClientProvider }im.ru
                </a>
               from "@tanstack/react-query";</li>
              
import { BrowserRouter,<li className="flex items-center gap-2">
                 Routes, Route } from "react-router<Icon name="Phone"-dom";
import className="h-4 w-4  Index from "./pages/Index";
import Nottext-primary" />
                <a hrefFound from "./pages/="tel:+78NotFound";

const queryClient = new001234567" className="text QueryClient();

const-gray-400  App = () => (hover:text-white
  <QueryClient transition-colors">Provider client={queryClient
                  +7 }>
    <To(800) 123oltipProvider>
      <Toaster />
      -45-67
                <Sonner /></a>
              
      <Browser</li>
            </ul>
            Router>
        <Routes>
          
            <div className="<Route path="/" elementmt-4 flex={<Index />} />
          <Route space-x-4"> path="/devices" element={
              <a href="#" className="text-<NotFound />} />
          <Route path="/gray-400 hoverfaq" element={:text-white"><NotFound />}
                <Icon name="Facebook" className=" />
          <Route path="/contacts" elementh-5 w-5"={<NotFound /> />
              } />
          </a>
              <Route path="/buy" element={<NotFoun<a href="#" className="d />} />
          text-gray-400 hover:text-white">
                {/* ADD ALL CUSTOM<Icon name="Instagram" className="h-5 w-5" ROUTES ABOVE THE />
               CATCH-ALL "*</a>
              " ROUTE */}<a href="#" className="text-gray-400
          <Route path hover:text-="*" element={white">
                <NotFound />}<Icon name="Twitter" />
        </Routes className="h-5 w-5">
      </B />
              rowserRouter>
    </TooltipProvider>
  </Query</a>
            </div>
          ClientProvider>
);</div>
        

export default App;</div>
        
</pp-write
        <div className>

<pp-="mt-8 write filepath="tailwind.config.tspt-8">
import { file border-t borderURLToPath } from-gray-700 "url";
import { text-center">
          <p dirname, resolve } from "path";
import type { Config } from "tailwindcss";

const __filename = fileURL className="text-grayToPath(import.-400 text-sm">
            &copy; {currentYear} Switch eSIM. Все права защmeta.url);ищены.
          
const __dirname = dirname</p>
        (__filename);

export</div>
      </div>
     default {
  dark</footer>
  );
};

export default Footer;
Mode: ["class"],
  content